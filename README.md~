- style is saved in `./dist/style.css`

# Compile Typescript to Javascript
`tsc -w ` recompiles everytime a file changes.

# Test
``` shell
cd json2typeform
npx http-server
# then go to the test directory
```
